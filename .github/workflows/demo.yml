name: Demo workflow

on: workflow_dispatch

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v3

    - name: configure .env-local
      run: |
        cd e2e
        ./script/demo.sh foo bar